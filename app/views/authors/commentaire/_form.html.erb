<<h3>Post a Comment</h3>
<%= form_for [ @authors, @commentaires ] do |f| %>
  
  <p>
    <%= f.label :body %><br/>
    <%= f.text_area :body %>
  </p>
<p>

 <%= calendar_date_select_tag "no_buttons", nil, :embedded => true, :buttons => false %>
   
</p>
  <p>
    <%= f.submit 'Submit' %>
  </p>
<% end %>
