

<% amount=7000 %>
<% b="jamel@yahoo.fr" %>
<% c="Spectacle de jamel" %>



<%= form_tag charges_path do %>
  <article>
    <label class="amount">
      <span>Amount: $815.00</span>
    </label>
  </article>

  <script src="https://checkout.stripe.com/checkout.js" class="stripe-button"

          data-key="<%= Rails.configuration.stripe[:publishable_key] %>"
          data-description="<%= c %>"
          data-email="<%= b %>"
          data-label="le show super"
          data-billing-address="true"
          data-amount="<%= amount %>"></script>

          <SELECT name="heure1">
    <OPTION VALUE="888815">888815</OPTION>
    <OPTION VALUE="816">816</OPTION>
    
  </SELECT>

<% end %>
