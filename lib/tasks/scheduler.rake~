desc "This task is called by the Heroku scheduler add-on"
task :update_feed => :environment do
  puts "Updating feed..."
  
  puts "done."
  a=User.find_by_id(1)
  a.update_attribute(:name,"cedric")


end

task :send_reminders => :environment do
  #User.send_reminders
end
